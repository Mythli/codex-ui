import { markdownCardType } from '@taylordb/learning-ui';

// 1. Export the Registry for the Demo App
// The UI library now provides the standard markdown card type out-of-the-box.
export const DEMO_CARD_REGISTRY = [markdownCardType];
